/** Primary navy horizontal mark (replace file in public/brand to update sign-in / core logo). */
export const LOB_CONCEPT_PRIMARY_SRC = "/brand/lob-concept-primary.png";
export const LOB_CONCEPT_PRIMARY_WIDTH = 1024;
export const LOB_CONCEPT_PRIMARY_HEIGHT = 799;

/** Marketing hero: logo panel + tagline + URL (entry page). Run `npm run brand:extract` after replacing lob-brand-hero.png. */
export const LOB_BRAND_HERO_SRC = "/brand/lob-brand-hero.png";
export const LOB_BRAND_HERO_WIDTH = 1024;
export const LOB_BRAND_HERO_HEIGHT = 540;

/** Full wordmark once per page (left panel: LOB + “Lumber One Board”). */
export const LOB_BRAND_LOCKUP_SRC = "/brand/lob-brand-lockup.png";
export const LOB_BRAND_LOCKUP_WIDTH = 512;
export const LOB_BRAND_LOCKUP_HEIGHT = 540;

/** Compact mark: LOB + wood “O” + rule only (nav, sidebar, favicon source). */
export const LOB_MARK_COMPACT_SRC = "/brand/lob-mark-compact.png";
export const LOB_MARK_COMPACT_WIDTH = 512;
export const LOB_MARK_COMPACT_HEIGHT = 248;
